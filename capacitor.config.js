// Named exports so Capacitor CLI (which uses require()) can read the config
// directly without needing to unwrap .default (Node 22+ ESM interop)
export const appId = 'com.fusionmania.app';
export const appName = 'Fusion Mania';
export const webDir = 'dist';
